type ButtonProps = {
    onClick?: () => void;
    children?: React.ReactNode;
}

function EditButton() {
    return (
        <button>
         Edit
        </button>
    )
}

export default EditButton
